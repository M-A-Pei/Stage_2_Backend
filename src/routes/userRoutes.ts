import { Router,  } from "express";

const userRoutes = Router()

userRoutes.get("/", )
userRoutes.get("/:id", )
userRoutes.post("/", )
userRoutes.put("/:id", )
userRoutes.delete("/:id", )

export default userRoutes